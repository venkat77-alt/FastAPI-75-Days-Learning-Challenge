
# Day 2 — Route Controllers, Path Parameters & Query Filters 🚀

## Overview

Day 2 of my FastAPI learning challenge focused on API routing and handling different types of parameters in FastAPI.

The goal was to understand how API routes are structured and how client-provided values can be received, typed, and handled by backend endpoints.

---

## 📚 Topics Covered

- Route Controllers
- RESTful URL structures
- Path Parameters
- Python Type Hints
- Query Parameters
- Optional Parameters
- Default Parameters
- Parameter Validation
- `Path()`
- `Query()`
- Search and filtering through query parameters

---

## 💻 Practical Implementation

For today's practice, I organized the API code into separate Python files instead of keeping everything inside a single file.

```text
day_02/
│
├── main.py
├── products.py
└── orders.py
